#include "monty.h"

/**
 * _comment - ignore comment line
 * @line: reps line
 * Return: nothing
 */

void _comment(const char *line)
{
	if (line == NULL || line[0] == '\0' || line[0] == '#')
		return;
}
